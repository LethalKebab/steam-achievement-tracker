# 本地配置:Steam API Key / SteamID64 / Notion token 都在这里,绝对不能提交
config.json

# 本地数据库(SQLite + WAL 临时文件)
data/

# CSV 导出的中间文件
exports/

# 备份 zip(`node tracker.js backup` / 设置页的「立即备份」的默认落点)。
# **这是仓库里最不能进版本控制的东西**:里面装着整个 config.json,也就是
# 明文的 Steam API Key、Notion token 和 AI 密钥,而这个仓库是公开的。
# 数据库和攻略也都在里面。
backups/

# 用户自己的攻略,连同它的三个历史目录:`.drafts/`(AI 生成但没通过校验的草稿)、
# `.migrated/`(搬去 Notion 之后的本地留底)、`.backups/`(覆盖前存下的原文)。
# **这个仓库是公开的**,而这些都是本机内容 —— 正文是个人的玩法记录,历史目录里
# 叠的是同一份攻略的旧版本,收进来等于每重写一次就多一份永远不会再更新的副本;
# 草稿更不能收,没验过的内容不该有任何一条路径流向别人。
# 目录由程序按需创建,读的地方都容忍它不存在(lib/guides.js 的 syncGuidesFromMarkdown),
# 打包也不带(launcher/package.json 的 extraResources 是白名单),所以不需要占位文件
guides/

# launcher/ 的 Electron 依赖,以及打包产物(仓库根目录的 dist/)——体积大(上百 MB),不该进仓库
launcher/node_modules/
dist/

# 本机专属,内容都是本机绝对路径,分发给别人的包永远不该带:
# - local.config.json:把打包版指回一份已有的 CLI 数据(见 lib/config.js 的 TRACKER_DATA_DIR)
# - *.lnk:build 时生成的、指向 dist/ 里那个 exe 的快捷方式
launcher/local.config.json
*.lnk
